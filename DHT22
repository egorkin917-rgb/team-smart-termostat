from machine import Pin
import dht
import time

sensor = dht.DHT22(Pin(15))

while True:
    try:
        sensor.measure()
        print(sensor.temperature())
    except:
        pass
    
    time.sleep(5)
